# Bautag 2026-09-26 (Samstag, W0) — Tagesbericht

**Orchestrator:** Opus 5.5 in der Rolle des Architekten (Specs, Orchestrierung, Merge). Neue Sitzung nach
`docs/bautage/uebergabe-2026-09-25-abend.md`, Start auf Integrationsbranch-Kopf `92e8e58` (Sitzungsbeginn am 25.09. abends).
**Auftrag:** nur Scheibe 080, Restbudget rund 38 USD, schlanker Review-Modus (AGENTS.md Regel 3).
**Harte Grenzen:** kein Deploy ohne Go, jeder Commit mit `[skip netlify]`, keine echten Daten, keine Zugangsdaten.

## Gemergte Scheiben

| Scheibe | PR | Squash | Bau · Modell | Review · Modell | Runden | Befunde (b/m/n) | Token (Bau + Review) | Tore |
|---|---|---|---|---|---|---|---|---|
| 080 Sprecher-Zustandstabelle R-SPK, Redezeit und Art zurückgebaut | [#39](https://github.com/ypoxx/hvworkflow/pull/39) | `252f3fe` | Opus 5.5 (drei Rückfragen an den Architekten) | Opus 5.5 (R1 + enge Nachprüfung) + Codex (ein Lauf, ohne Befund) | 1 + Nachprüfung | R1 0/2/3 + 1 nit: `reason` ungeprüft ins Log (T-G1-T-02) → behoben; Spec ohne Bedrohungs-IDs → Nachtrag; Rest → Folgeliste | 163 645 + 87 660 = 251 305 | `pnpm gates` grün auf `86cdf0d`, e2e 117/117 auf `c4e5d37`, CI grün auf `d386f58` |

## Befunde und Abweichungen

- **080 geteilt.** Nach Lesen des Codes war der Planeintrag für eine Scheibe zu groß. Korpus 28/230 und Umsortierung mit
  Begründung stehen als **080b** im Plan (Abschnitt 5). Das Löschen der Vertragsfelder geht nach ADR 0015 in 0.4.0 (043),
  weil es nicht additiv ist und 0.3.1 an 028 hängt.
- **Befund beim Lesen:** `updateSpeaker` prüfte keinen Statuswechsel (auch `finished → speaking` ging durch). Jetzt
  Tabelle, 409 mit `R-SPK-00`.
- **Drei Rückfragen des Implementierers**, jeweils vor einem Ausweichen (gut): Regelregister-Muster und Konfliktregel,
  Fingerabdruck des Seeds (alter Seed ohne die beiden Felder bytegleich zum neuen), i18n-Schlüsselzahl und nicht
  dokumentierte 409. Jede Antwort als Nachtrag in der Spec. Ursache: Files allowed zu eng geschnitten, weil Tests mit
  festen Zahlen und Fingerabdrücken nicht mitgelesen wurden.
- **Push war zunächst gesperrt** (Rechte-Filter der Sitzung); der Eigentümer hat ihn freigegeben.

## Beim Eigentümer offen

Unverändert aus der Übergabe; zusätzlich: Abrechnung der Subagenten-Token prüfen (heute 251 305, Kopfsitzung nicht gezählt).
